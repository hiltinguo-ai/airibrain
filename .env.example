# Copy to .env - it is loaded automatically and .gitignored, so the key never
# travels through shell history or chat. Rotate any key that has been pasted anywhere.
ANTHROPIC_API_KEY=sk-ant-your-key-here
# VCBRAIN_MODEL=claude-sonnet-4-5
# VCBRAIN_MAX_SEARCHES=6
# VCBRAIN_MAX_RETRIES=3
# VCBRAIN_TIMEOUT_S=180
